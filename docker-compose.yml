services:
  lb:
    build:
      context: .
      dockerfile: Dockerfile.node
    hostname: lb
    container_name: lb
    networks:
      - pulsecheck-net
    labels:
      - "project=pulsecheck"
      - "role=lb"
    restart: unless-stopped

  app1:
    build:
      context: .
      dockerfile: Dockerfile.node
    hostname: app1
    container_name: app1
    networks:
      - pulsecheck-net
    labels:
      - "project=pulsecheck"
      - "role=app"
    restart: unless-stopped

  app2:
    build:
      context: .
      dockerfile: Dockerfile.node
    hostname: app2
    container_name: app2
    networks:
      - pulsecheck-net
    labels:
      - "project=pulsecheck"
      - "role=app"
    restart: unless-stopped

  db:
    build:
      context: .
      dockerfile: Dockerfile.node
    hostname: db
    container_name: db
    networks:
      - pulsecheck-net
    labels:
      - "project=pulsecheck"
      - "role=db"
    restart: unless-stopped

networks:
  pulsecheck-net:
    name: pulsecheck-net
    driver: bridge
